global:
    import_to_db: True
    add_pks: True
    db_settings:
      host: beta.dataafrica.io
      user: postgres
      password_env_var: DA_DB_PW
      db_name: data_africa
      type:
        value_of_production: bigint
        adm0_id: integer
        adm1_id: integer
        svyl1cd: integer
        year: integer
        start_year: integer
        regcode: text
